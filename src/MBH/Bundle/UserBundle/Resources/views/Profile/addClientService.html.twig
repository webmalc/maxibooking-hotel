{% extends "MBHUserBundle::layout.html.twig" %}

{% set title = 'view.personal_account.title'|trans %}

{% block content %}
    {{ parent() }}
    <div class="tab-content">
        <div class="tab-pane active" id="general_info">
            {{ form_start(form, {'action': path('add_client_service'), 'attr': {'class': 'form-horizontal'}}) }}
            {{ form_widget(form) }}
            {% include 'MBHBaseBundle:Actions:new.html.twig' %}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block scripts %}
    {{ parent() }}
    <script>
        var services = JSON.parse('{{ serializedServices|raw }}');
        console.log(services);
    </script>
{% endblock %}
