<button {{ data.disabled is defined and data.disabled ? 'disabled' }} class="btn btn-{{ data.disabled is defined and data.disabled ? 'danger' : 'warning' }} {{ data.buttonClasses is defined ? data.buttonClasses }}"><i class="fa fa-ruble"></i>&nbsp;{{ data.buttonText is defined ? data.buttonText }}</button>