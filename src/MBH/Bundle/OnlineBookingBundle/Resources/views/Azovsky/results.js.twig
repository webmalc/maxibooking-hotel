(function () {
    var head  = document.getElementsByTagName('head')[0];
    var mbCssId = {{ ( "'%s%s'"| format('mb-results-css', fileName)) | raw }} ;
    var mbJsId = {{ ( "'%s%s'"| format('mb-results-js', fileName)) | raw }};

    if (!document.getElementById(mbCssId))
    {
        var link  = document.createElement('link');
        link.id   = mbCssId;
        link.rel  = 'stylesheet';
        link.type = 'text/css';
        link.href = '{{ absolute_url(asset("build/%s.css"|format(fileName))) | raw }}';
        link.media = 'all';
        head.appendChild(link);
    }

    if (!document.getElementById(mbJsId))
    {
        var script  = document.createElement('script');
        script.id   = mbJsId;
        script.src = '{{ absolute_url(asset("build/%s.js"|format(fileName))) | raw }}';
        head.appendChild(script);
    }
})();
