<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h4 class="modal-title">{{ 'modal.form.delete.reasons.confirmation'|trans({}, 'MBHPackageBundle') }}</h4>
        </div>
        <div class="modal-body">
            {% form_theme form 'MBHPackageBundle:Documents:modalFormFields.html.twig' %}
            {{ form_start(form, {'action': path('package_delete'), 'method': 'POST', 'attr': {'class': 'form-horizontal'}}) }}
            {{ form_widget(form) }}

            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'csv.form.modal_close'|trans }}</button>
                <button type="button" class="btn btn-primary reason_delete_package">Отправить</button>
            </div>
            {{ form_end(form) }}

        </div>

    </div>
</div>

