<div class="well well-sm we">
    <ul class="nav nav-pills">
        <li role="presentation" {% if active == 1 %} class="active" {% endif %}><a
                    href="{{ path('package_order_tourist_edit', {id: entity.id, packageId: package.id}) }}"><i class="fa fa-male"></i> {{ 'order.tabs.individual'|trans}}
                {% if entity.mainTourist is null %}
                    <span class="badge">{{ 'order.tabs.no'|trans}}</span>
                {% endif %}
            </a>
        </li>
        <li role="presentation" {% if active == 2 %} class="active" {% endif %}><a
                    href="{{ path('package_order_organization_edit', {id: entity.id, packageId: package.id}) }}"><i class="fa fa-users"></i> {{ 'order.tabs.organization'|trans}}
                {% if entity.organization is null %}
                    <span class="badge">{{ 'order.tabs.no'|trans}}</span>
                {% endif %}
            </a>
        </li>
    </ul>
</div>