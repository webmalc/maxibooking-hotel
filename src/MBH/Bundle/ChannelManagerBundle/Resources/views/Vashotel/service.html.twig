{% extends 'MBHChannelManagerBundle:Vashotel:layout.html.twig' %}

{% set small_title = "настройки" %}

{% block content %}

    {% include 'MBHChannelManagerBundle::tabs.html.twig' with {'active': 4, 'entity': doc, 'base': 'booking'} %}

    <div class="tab-content">

        <table class="table table-striped table-hover table-condensed not-auto-datatable">
            <thead>
            <tr>
                <th class="td-lg">{{ 'views.booking.service.service'|trans({}, 'MBHChannelManagerBundle') }}</th>
                <th class="td-lg">{{ 'views.booking.service.code'|trans({}, 'MBHChannelManagerBundle') }}</th>
                <th>ID</th>
            </tr>
            </thead>
            <tbody>
            {% for service in doc.services %}
                <tr>
                    <td>{{ service.service.name }} <a class="external" target="_blank" href="{{ path('price_service_category_entry_edit', {'id': service.service.id}) }}"><i class="fa fa-external-link"></i></a></td>
                    <td>{{ service.service.code }}</td>
                    <td>{{ service.serviceId }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="tab-pane active" id="home">
            <div id="actions" class="navbar navbar-default navbar-fixed-bottom">
                <div class="container-fluid">
                    <ul class="nav navbar-nav">
                        <li><button type="button" data-toggle="tooltip" title="Синхронизироваться с Booking.com" onclick="location.href = '{{ path('channel_manager_sync', {'name': 'booking'}) }}'" class="btn btn-success navbar-btn"><i class="fa fa-share-square-o"> </i> {{ 'views.booking.service.sync'|trans({}, 'MBHChannelManagerBundle') }}</button></li>
                        <li><button type="button" onclick="location.href = '{{ title_url|default(path('_welcome'))}}'" class="btn btn-default navbar-btn"><i class="fa fa-ban"> </i> {{ 'views.booking.service.cancel'|trans({}, 'MBHChannelManagerBundle') }}</button></li>
                    </ul>
                </div>
            </div>
            {% if doc %}
                {% include 'MBHBaseBundle:Partials:entityInfo.html.twig' with {'entity': doc, 'logs': logs} %}
            {%  endif %}
        </div>
    </div>
{% endblock %}