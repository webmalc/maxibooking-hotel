<?xml version="1.0" encoding="utf-8"?>
<request>
    {% include 'MBHChannelManagerBundle:Booking:credentials.xml.twig' with {'config': config } %}

    {% for room in rooms %}
        <room id="{{ room['syncId'] }}">
            {% for rate in rates %}
                <date from ="{{ 'now'|date('Y-m-d') }}" to="{{ 'now'|date_modify("+5 years")|date('Y-m-d') }}">
                    <rate id="{{ rate['syncId'] }}" />
                    <roomstosell>0</roomstosell>
                </date>
            {% endfor %}
        </room>
    {% endfor %}

</request>