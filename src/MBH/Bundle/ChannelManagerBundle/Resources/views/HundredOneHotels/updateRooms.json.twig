{
    "api_key" : "wpYofbd5ns4Y2EGfGHv0",
    "hotel_id" : "{{ config.hotel.id }}",
    "service" : "set_calendar",
    "data" : [
        {% for date, dayQuotas in quotas %}
        {
            "day" : "{{ date }}",
            "quota" : {
                {% for roomTypeID, roomTypeQuota in dayQuotas %}
                    "{{ roomTypeID }}" : "{{ roomTypeQuota }}"
                {% endfor %}
                }
        },
        {% endfor %}
    ]
}