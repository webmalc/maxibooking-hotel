{% extends 'MBHWarehouseBundle:Warehouse:layout.html.twig' %}

{% set small_title = 'warehouse.items.new'|trans() ~ " <" ~ entity.name ~ ">" %}
{% set layout = 'box' %}

{% block content %}
    {{ form_start(form, {'action': path('warehouse_category_entry_create', {'id': entity.id}), 'attr': {'class': 'form-horizontal'}}) }}
    {{ form_widget(form) }}
    {% include 'MBHBaseBundle:Actions:new.html.twig' %}
    {{ form_end(form) }}
{% endblock %}

