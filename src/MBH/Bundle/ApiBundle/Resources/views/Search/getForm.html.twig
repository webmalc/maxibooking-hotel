{% extends 'MBHApiBundle:Search:layout.html.twig' %}

{% block content %}
    {{ form_start(form, {'action': '#', 'attr': {'target': '_top', 'class': 'mbh-search-form'}}) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <button type="submit" class="btn btn-default">{{ 'api.search.form.submit'|trans }}</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
