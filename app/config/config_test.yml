imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.php_bridge
    profiler:
        enabled: true

swiftmailer:
    disable_delivery: true

parameters:
    mbh.mongodb:
          url: "mongodb://%mongodb_host%/%mongodb_database%%mongodb_options%"
          db : "%mongodb_database%"
    mbh_cache:
        is_enabled: true
        prefix: mbh
        lifetime: 7
        logs: false
        search:
            months: 6
            max_duration: 21
            min_duration: 3
security:
    firewalls:
        main:
            http_basic: ~
            form_login: false
            remember_me: false

liip_functional_test:
  authentication:
    username: "admin"
    password: "admin"

doctrine_mongodb:
    connections:
        default:
            server: "mongodb://%mongodb_host%/%mongodb_database%%mongodb_options%"